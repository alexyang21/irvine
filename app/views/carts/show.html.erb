<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>

<h2>Your Cart</h2>

<ul>
  <% @cart.items.each do |item| %>
    <li><%= item.quantity %> &times; <%= item.menu.name %></li>
  <% end %>
</ul>